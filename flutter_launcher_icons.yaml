flutter_launcher_icons:
  android: true
  ios: true
  image_path: "assets/icon.png"

  # Android 配置
  android:
    generate: true
    image_path: "assets/icon.png"

  # iOS 配置
  ios:
    generate: true
    image_path: "assets/icon.png"

  # Web 配置
  web:
    generate: true
    image_path: "assets/icon.png"

  # Windows 配置
  windows:
    generate: true
    image_path: "assets/icon.png"

  # macOS 配置
  macos:
    generate: true
    image_path: "assets/icon.png"

  # Linux 配置
  linux:
    generate: true
    image_path: "assets/icon.png"
